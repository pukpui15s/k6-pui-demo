# Server (บน Render จะมี PORT ให้อัตโนมัติ)
PORT=3000

# Prefix ชื่อ metric (เช่น myapp → myapp_disk_free_bytes, tester_demo → tester_demo_disk_free_bytes)
METRICS_PREFIX=myapp

# Disk metrics (path ที่จะวัด disk free/total)
METRICS_DISK_PATH=/
METRICS_DISK_INTERVAL_MS=15000

# Memory/เวลาอัปเดต metrics (จำนวนแรม + เวลา save ล่าสุด)
# อัปเดต memory + last_update ทุกกี่ ms (default 5000 = 5 วินาที)
METRICS_MEMORY_INTERVAL_MS=5000
